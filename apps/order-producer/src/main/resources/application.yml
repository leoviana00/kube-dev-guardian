spring:
  application:
    name: order-producer

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
        
  endpoint:
    health:
      show-details: never