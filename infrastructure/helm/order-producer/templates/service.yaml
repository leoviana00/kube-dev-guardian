apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-producer.fullname" . }}
  labels:
    {{- include "order-producer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "order-producer.selectorLabels" . | nindent 4 }}
