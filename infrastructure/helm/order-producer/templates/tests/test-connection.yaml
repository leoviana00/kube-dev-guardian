apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order-producer.fullname" . }}-test-connection"
  labels:
    {{- include "order-producer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "order-producer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
