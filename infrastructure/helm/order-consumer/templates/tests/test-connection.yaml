apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "order-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "order-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
