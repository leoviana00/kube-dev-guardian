apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-consumer.fullname" . }}
  labels:
    {{- include "order-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "order-consumer.selectorLabels" . | nindent 4 }}
